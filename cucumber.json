{
  "default": {
    "format": ["@cucumber/pretty-formatter"],
    "formatOptions": {
      "colorsEnabled": true
    },
    "requireModule": ["ts-node/register"],
    "require": ["features/**/*.ts"]
  },
  "parallel": {
    "parallel": 5,
    "format": ["progress"],
    "formatOptions": {
      "colorsEnabled": true
    },
    "requireModule": ["ts-node/register"],
    "require": ["features/**/*.ts"]
  }
}
