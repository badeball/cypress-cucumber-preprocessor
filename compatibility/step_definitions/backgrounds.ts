import { Given, When, Then } from "@badeball/cypress-cucumber-preprocessor";

Given("an order for {string}", () => {
  // no-op
});

When("an action", () => {
  // no-op
});

Then("an outcome", () => {
  // no-op
});
