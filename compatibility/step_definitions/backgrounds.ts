import { Given, Then, When } from "@badeball/cypress-cucumber-preprocessor";

Given("an order for {string}", () => {
  // no-op
});

When("an action", () => {
  // no-op
});

Then("an outcome", () => {
  // no-op
});
